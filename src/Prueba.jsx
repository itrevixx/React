/* import { useState } from "react";

const Prueba = () => {
    const [inputText, setInputText] = useState();


    return (
        <div>
            <input type="text" onChange={e => setInputText(e.target.value)} />
            <div>{inputText * 2}</div>
        </div>
    );
}

export default Prueba; */

/* import { useState } from "react";

const Prueba = () => {
    const [inputText, setInputText] = useState();


    return (
        <div>
            <input type="text" onChange={e => setInputText(e.target.value)} />
            <div>{inputText && inputText * 9 / 5 + 32}</div>
        </div>
    );
}

export default Prueba; */
